## 1. Database Migration
- [x] 1.1 Create migration file to add `is_hold_point` column to `itp_checks`

## 2. Validation
- [ ] 2.1 Run migration locally with `supabase migration up`
- [ ] 2.2 Verify column exists in database
